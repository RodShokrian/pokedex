import React from 'react';
import { Provider } from 'react-redux';

const Root = () => (
  <h1>Hello, React!</h1>
  // <Provider store={ store }>
    // <App />
  // </Provider>
);

export default Root;
